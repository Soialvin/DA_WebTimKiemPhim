@using DAM.Models
@model IEnumerable<TheLoai>

<nav>
    <ul class="nav navbar-nav">
        <li class="active"><a href="@Url.Action("Main","Main")">Trang chủ</a></li>
        <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Thể loại <b class="caret"></b></a>
            <ul class="dropdown-menu multi-column columns-3">
                <li>
                    <div class="col-sm-10">
                        <ul class="multi-column-dropdown">
                            @foreach (var item in Model)
                            {
                                <li><a href="@Url.Action("PhanLoaiPhim", "QLPhim", new { MaTL = item.MaTL })">@item.TenTL</a></li>
                            }
                        </ul>
                    </div>
                </li>
            </ul>
        </li>
        <li><a href="@Url.Action("TaoMoi","Ve")">Mua vé</a></li>
        <li><a href="@Url.Action("AllKhuyenMai", "KhuyenMai")">Tin tức</a></li>
    </ul>
</nav>
